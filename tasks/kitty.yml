- name: Download Kitty 
  get_url:
      url: https://sw.kovidgoyal.net/kitty/installer.sh 
      dest: /tmp/kitty.sh
  tags:
    - install 
    - kitty

- name: Install Kitty
  become: yes
  shell: 
    cmd: sh ./kitty.sh -y
    chdir: /tmp 
  tags:
    - install 
    - kitty
